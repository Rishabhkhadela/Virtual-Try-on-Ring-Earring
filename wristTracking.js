// wristTracking.js — STUB
// Real implementation hasn't landed yet. These no-op exports exist so main.js's
// static `import` statement resolves and the page actually loads. Replace with
// real tracking logic when ready (same signatures below).

export function initializeWristTracking(/* scene, camera, mapToOrthographicSpace */) {
  // no-op
}

export function updateWristTracking(/* wristLandmarks, wristWorldLandmarks, isRightHand */) {
  // no-op
}
